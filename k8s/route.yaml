apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: metrics-submission
  name: metrics-submission
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: metrics-submission
    weight: 100